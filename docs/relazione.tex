\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[italian]{babel}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{multicol}

\geometry{margin=2.5cm}
\setlength{\headheight}{14.5pt}

\lstset{
    basicstyle=\ttfamily\footnotesize,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{green!60!black}\itshape,
    stringstyle=\color{red!80!black},
    identifierstyle=\color{black},
    numberstyle=\tiny\color{gray},
    emphstyle=\color{orange},
    showstringspaces=false,
    breaklines=true,
    frame=single,
    frameround=tttt,
    backgroundcolor=\color{gray!5},
    rulecolor=\color{gray!40},
    numbers=none,
    numbersep=8pt,
    captionpos=b,
    tabsize=4,
    xleftmargin=10pt,
    xrightmargin=5pt,
    framexleftmargin=5pt
}

% Definizione per il linguaggio DOT (Graphviz)
\lstdefinelanguage{DOT}{
    keywords={digraph, graph, node, edge, subgraph},
    sensitive=true,
    comment=[l]{//},
    morecomment=[s]{/*}{*/},
    morestring=[b]",
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{green!60!black}\itshape,
    stringstyle=\color{red!80!black}
}

% Definizione per JSON
\lstdefinelanguage{json}{
    basicstyle=\ttfamily\footnotesize,
    numbers=none,
    numberstyle=\tiny\color{gray},
    stepnumber=1,
    numbersep=8pt,
    showstringspaces=false,
    breaklines=true,
    frame=single,
    backgroundcolor=\color{gray!5},
    string=[s]{"}{"},
    stringstyle=\color{red!80!black},
    comment=[l]{:},
    commentstyle=\color{black},
    morecomment=[l]{//},
    morecomment=[s]{/*}{*/},
    literate=
     *{0}{{{\color{orange}0}}}{1}
      {1}{{{\color{orange}1}}}{1}
      {2}{{{\color{orange}2}}}{1}
      {3}{{{\color{orange}3}}}{1}
      {4}{{{\color{orange}4}}}{1}
      {5}{{{\color{orange}5}}}{1}
      {6}{{{\color{orange}6}}}{1}
      {7}{{{\color{orange}7}}}{1}
      {8}{{{\color{orange}8}}}{1}
      {9}{{{\color{orange}9}}}{1}
      {:}{{{\color{blue}{:}}}}{1}
      {,}{{{\color{blue}{,}}}}{1}
      {\{}{{{\color{purple}{\{}}}}{1}
      {\}}{{{\color{purple}{\}}}}}{1}
      {[}{{{\color{purple}{[}}}}{1}
      {]}{{{\color{purple}{]}}}}{1}
      {true}{{{\color{blue!80!black}true}}}{4}
      {false}{{{\color{blue!80!black}false}}}{5}
      {null}{{{\color{blue!80!black}null}}}{4}
}

\pagestyle{fancy}
\fancyhf{}
\rhead{peshmind}
\lhead{}
\rfoot{Pagina \thepage}

\title{
\includegraphics[width=0.3\textwidth]{img/logo.png} \\
\vspace{1cm}
\large \textcolor{red}{P}rolog-based \textcolor{red}{E}ngine for \textcolor{red}{S}ystem \& \textcolor{red}{H}ost \textcolor{red}{M}anagement and \textcolor{red}{In}ference \textcolor{red}{D}aemon \\
\vspace{1cm}
}
\author{Mirko Mariotti}
\date{Marzo 2026}

\begin{document}

\maketitle
\thispagestyle{empty}

\newpage
\tableofcontents
\newpage

\section{Introduzione}

peshmind è un sistema per la scoperta e l'analisi della topologia di una rete LAN che si basa esclusivamente
sugli indirizzi MAC osservati dagli switch di rete.
Il sistema combina un'applicazione CLI scritta in Go con un motore Prolog per identificare 
le connessioni di rete, gli switch interni/di accesso e i potenziali "ghost switch" (switch non censiti)
presenti nell'infrastruttura di rete.

\subsection{Motivazione e Obiettivi}

In reti complesse di grandi dimensioni, la documentazione della topologia fisica spesso diventa obsoleta 
o incompleta. peshmind affronta questo problema fornendo un sistema automatizzato che:

\begin{itemize}
    \item Ricostruisce la topologia di rete analizzando solo le tabelle MAC degli switch
    \item Identifica automaticamente switch non censiti ("ghost switch"), abilitando la possibilità di scoprire anomalie e dispositivi non autorizzati
    \item Distingue tra switch di core/distribuzione e switch di accesso
    \item Fornisce visualizzazioni grafiche della topologia inferita
\end{itemize}

\section{Architettura del Sistema}

\subsection{Componenti Principali}

Il sistema peshmind è composto da tre componenti principali che interagiscono tra loro:

\subsubsection{Applicazione CLI Go}

L'applicazione CLI \textbf{peshmind}, sviluppata in Go, coordina tutte le operazioni del sistema, comprese le
due altre componenti. I suoi sottocomandi, realizzati con il framework Cobra, gestiscono le seguenti funzionalità:

\begin{itemize}
    \item Recupero automatico delle tabelle MAC dagli switch fisici
    \item Conversione dei dati in fatti Prolog
    \item Avvio e gestione del motore Prolog
    \item Interrogazione del sistema di inferenza
    \item Generazione di visualizzazioni DOT
    \item Simulazione di topologie di rete
\end{itemize}

\subsubsection{Motore di Inferenza Prolog}

Il motore Prolog (SWI-Prolog) analizza i fatti relativi agli indirizzi MAC e inferisce la topologia di rete attraverso regole logiche. Utilizza la libreria Pengines per esporre un endpoint HTTP RESTful.

\subsubsection{Knowledge Base}

La base di conoscenza memorizza i dati degli switch e degli indirizzi MAC sotto forma di fatti Prolog:

\begin{itemize}
    \item \texttt{switch(MacAddr)} - Dichiara l'esistenza di uno switch
    \item \texttt{switchname(MacAddr, Name)} - Associa un MAC a un nome
    \item \texttt{seen(SwitchMac, DeviceMac, Port)} - Registra MAC visti su porte specifiche
\end{itemize}

\subsection{Due possibili flussi di lavoro}

Il sistema supporta due flussi di lavoro principali, quello di analisi di una rete esistente e 
quello di simulazione di topologie di rete. Il secondo è particolarmente utile per testare e validare le regole
di inferenza Prolog prima di applicarle a dati reali. \\
A grandi linee, i flussi di lavoro seguono i passaggi descritti di seguito, l'argomento sarà approfondito nelle sezioni
successive con esempi pratici e dettagli tecnici.

\begin{multicols}{2}
        \textbf{Analisi Rete Esistente}
        \begin{enumerate}
            \item Configurazione switch in \texttt{peshmind.json}
            \item Acquisizione tabelle MAC via SSH o telnet
            \item Conversione dati in fatti Prolog
            \item Caricamento KB e inferenza
            \item Interrogazione e visualizzazione risultati
        \end{enumerate}
\columnbreak
        \textbf{Simulazione Topologia di Rete}
        \begin{enumerate}
            \item Creazione file DOT con topologia desiderata
            \item Esecuzione simulazione per generare fatti Prolog
            \item Caricamento KB e inferenza
            \item Interrogazione e visualizzazione risultati
            \item Iterazione per testare diverse configurazioni
        \end{enumerate}
\end{multicols}

\subsection{Struttura del Codice}

I sorgenti del progetto sono organizzati nella seguente struttura:

\begin{verbatim}
peshmind/
|-- main.go              # Entry point dell'applicazione
|-- cmd/                 # Comandi Cobra
|   |-- root.go         # Comando radice
|   |-- list.go         # Elenca switch configurati
|   |-- update.go       # Aggiorna dati switch
|   |-- serve.go        # Avvia motore Prolog
|   |-- query.go        # Interroga il motore
|   |-- dot.go          # Genera file DOT
|   +-- simulate.go     # Simula topologie
|-- pkg/peshmind/        # Logica di business
|   |-- peshmind.go     # Configurazione
|   |-- switch.go       # Gestione switch
|   |-- spawn.go        # Avvio motore Prolog
|   +-- simulate.go     # Logica di simulazione
+-- kbpool/              # Knowledge base Prolog
    |-- rules.pl        # Regole di inferenza
    |-- server.pl       # Server HTTP Pengines
    +-- createkb.sh     # Script creazione KB
\end{verbatim}
\subsection{Tecnologie Utilizzate}

\subsubsection{Go}

Il linguaggio Go è stato scelto per l'applicazione CLI in quanto offre performance eccellenti,
 consente la compilazione statica con generazione di un singolo binario facilmente deployable, 
 fornisce un supporto eccellente per la programmazione concorrente e mette a disposizione un ricco
  ecosistema di librerie pronte all'uso. \\
Le librerie Go che sono state utilizzate includono:
\begin{itemize}
    \item \textbf{Cobra}: Framework CLI per comandi e flag
    \item \textbf{Pengine Client}: Client Go per interrogare Pengines
    \item \textbf{go-graphviz}: Parsing e generazione di grafi DOT
    \item \textbf{golang.org/x/term}: Input sicuro password
\end{itemize}
\subsubsection{SWI-Prolog}

SWI-Prolog fornisce il motore di inferenza grazie a un potente sistema di inferenza logica, alla 
libreria Pengines per l'esposizione di API HTTP, a una sintassi dichiarativa particolarmente adatta alla 
definizione di regole complesse e a ottime prestazioni anche su knowledge base di grandi dimensioni.

\section{Installazione}
\subsection{Prerequisiti}

Per compilare ed eseguire peshmind, sono necessari i seguenti software

\begin{itemize}
    \item Git (per clonare il repository)
    \item Go 1.20 o superiore (per compilare l'applicazione CLI)
    \item SWI-Prolog 8.0 o superiore (per il motore di inferenza)
    \item Graphviz (per generazione visualizzazioni)
\end{itemize}

\subsection{Installazione}

l'applicazione è scaricabile dal repository GitHub del progetto \\

\begin{lstlisting} 
    git clone https://github.com/mmirko/peshmind.git
\end{lstlisting}

Dopo aver clonato il repository, è possibile compilare l'applicazione CLI con il comando: \\
\begin{lstlisting}
    cd peshmind
    go build -o peshmind main.go
\end{lstlisting}

Il binario compilato sarà disponibile nella directory corrente e potrà essere eseguito da terminale.
In alternativa, è possibile copiare il binario in una directory presente nel PATH per renderlo accessibile globalmente. \\

In questo caso è necessario assicurarsi di mantenere la directory \texttt{kbpool} e i file Prolog al suo interno, in quanto
sono necessari per il corretto funzionamento del sistema.

\section{Utilizzo}
\subsection{Comandi CLI generali}

Gran parte dei comandi CLI di peshmind sono utilizzabili sia per l'analisi di una rete reale che per la 
simulazione di topologie, in quanto operano sui fatti Prolog generati da entrambe le modalità. \\
Per la corretta esecuzione dei comandi, in ogni modalità, sono necessari: il file di configurazione \texttt{peshmind.json} 
e i fatti Prolog nella directory \texttt{kbpool} (generati da \texttt{update} o \texttt{simulate} a seconda della modalità)
che contiene anche le regole di inferenza (\texttt{rules.pl}) e il server Pengines (\texttt{server.pl}).

\subsubsection{List}

Il comando \texttt{list} visualizza tutti gli switch configurati con i loro dettagli:

\begin{lstlisting}[language=bash]
peshmind list
\end{lstlisting}

\subsubsection{Serve}

Il comando \texttt{serve} avvia il motore Prolog con endpoint HTTP:

\begin{lstlisting}[language=bash]
peshmind serve --kbpool kbpool
\end{lstlisting}

Una volta avviato, il motore è pronto a ricevere query e a rispondere con i risultati dell'inferenza logica.
Tecnicamente, questo comando lancia e mantiene attivo un processo Prolog che carica i fatti e le regole dalla directory
\texttt{kbpool} e espone un endpoint HTTP RESTful per l'interrogazione. Il processo rimane attivo fino a quando non viene
interrotto manualmente con CTRL+C o con un comando di terminazione del processo.

\subsubsection{Query}

Il comando \texttt{query} permette di interrogare il motore Prolog:

\begin{lstlisting}[language=bash]
# Trova tutti gli switch
peshmind query "switch(X)"

# Trova switch direttamente connessi
peshmind query "directn(X,Y)"

# Identifica ghost switch
peshmind query "ghostn(X,Y)"
\end{lstlisting}

In questo caso, il comando invia la query all'istanza di peshmind che esegue il sottocomando \texttt{serve} e riceve
i risultati, che vengono poi formattati e stampati a terminale. \\
I due sottocomandi \texttt{query} e \texttt{serve} sono progettati per essere utilizzati insieme, 
dove \texttt{serve} mantiene attivo il motore Prolog e \texttt{query} invia richieste di inferenza. \\
Per dettagli sulle regole di inferenza implementate, si rimanda alla sezione \ref{sec:inference}.

\subsubsection{Dot}

Il comando \texttt{dot} genera un file DOT che rappresenta la topologia inferita. Tale file può essere visualizzato 
con i tool di Graphviz (dot, fdp, neato, ecc.) per ottenere una rappresentazione grafica della rete: \\

\begin{lstlisting}[language=bash]
peshmind dot > network.dot
dot -Tpng network.dot -o network.png
\end{lstlisting}

Il file DOT generato include:
\begin{itemize}
    \item Cluster per ogni switch con le relative porte
    \item Collegamenti fisici tra le porte
    \item Ghost switch evidenziati con stile differente
    \item Codifica colori per migliore leggibilità
\end{itemize}

\subsection{Comandi per l'analisi di una rete reale}

\subsubsection{Update}

Il comando \texttt{update} recupera le tabelle MAC dagli switch: \\

\begin{lstlisting}[language=bash]
# Aggiorna uno switch specifico
peshmind update --switch switch-01

# Aggiorna tutti gli switch
peshmind update --switch all
\end{lstlisting}

Questo comando:
\begin{enumerate}
    \item Si connette allo switch via SSH o telnet usando le credenziali fornite nel file di configurazione
    \item Processaa un template di comandi specifico per il modello di switch e, utilizzando expect, interagisce con la shell dello switch per eseguire i comandi necessari a ottenere la tabella MAC
    \item Analizza l'output con espressioni regolari
    \item Genera fatti Prolog nel formato \texttt{seen/3}
    \item Salva il file nella directory \texttt{kbpool}
\end{enumerate}

\subsection{Comandi per la simulazione di topologie}

\subsubsection{Simulate}

Il comando \texttt{simulate} permette di testare topologie:

\begin{lstlisting}[language=bash]
peshmind simulate --simname topology.dot \
    --emit-dot output.dot \
    --output facts.pl
\end{lstlisting}

Anch'esso. alla pari del comando \texttt{update}, genera fatti Prolog, ma invece di connettersi a switch reali,
genera i fatti in maniera programmatica a partire da un file DOT che descrive la topologia desiderata. Supporta
attributi speciali per identificare ghost switch, specificare MAC address e porte di connessione. Dettagli sulla
struttura del file DOT e sugli attributi supportati sono forniti nella sezione \ref{sec:conf}.

\section{Inferenza Logica con Prolog}
\label{sec:inference}

Il cuore del sistema è il motore di inferenza Prolog, che utilizza i fatti generati per dedurre la topologia di rete.
In questa sezione vengono descritte le principali regole di inferenza implementate in Prolog.

\subsection{Predicato far/2}

Due switch sono "lontani" se sono connessi a un terzo switch sulla stessa porta:

\begin{lstlisting}[language=Prolog]
far(X,Y) :- 
    seen(X,K,MIDPORTX), 
    seen(Y,K,MIDPORTY),
    seen(X,Y,PORTX),
    seen(Y,X,PORTY),
    switch(K), switch(X), switch(Y), 
    X \= Y, Y \= K, X \= K, 
    PORTX == MIDPORTX,
    PORTY == MIDPORTY.
\end{lstlisting}

Questo predicato identifica la presenza di uno switch intermedio tra due switch.

\subsection{Predicato direct/2}

Due switch sono direttamente connessi se non sono lontani:

\begin{lstlisting}[language=Prolog]
direct(X,Y) :- \+ far(X,Y).
\end{lstlisting}

\subsection{Predicato directp/4}

Connessione diretta con informazioni sulle porte:

\begin{lstlisting}[language=Prolog]
directp(X,Y,PORTX,PORTY) :- 
    seen(X,Y,PORTX),
    seen(Y,X,PORTY),
    \+ far(X,Y).
\end{lstlisting}

\subsection{Predicato ghost/2}

Identifica ghost switch tra due switch direttamente connessi:

\begin{lstlisting}[language=Prolog]
ghost(X,Y) :- 
    directp(X,Y,PORTX,PORTY),
    seen(X,Z,PORTX),
    seen(Y,Z,PORTY),
    \+ switch(Z).
\end{lstlisting}

Se due switch vedono lo stesso MAC (non switch) sulle porte di interconnessione, esiste un ghost switch.

\subsection{Predicato internalswitch/1}

Identifica switch interni (core/distribuzione):

\begin{lstlisting}[language=Prolog]
internalswitch(X) :- 
    seen(X,Y,PORT1),
    seen(X,Z,PORT2),
    switch(X), switch(Y), switch(Z), 
    X \= Y, X \= Z, Y \= Z,
    PORT1 \= PORT2.
\end{lstlisting}

Uno switch è interno se è connesso ad almeno due switch diversi.

\subsection{Predicato edgeswitch/1}

Identifica switch di accesso:

\begin{lstlisting}[language=Prolog]
edgeswitch(X) :- switch(X), \+ internalswitch(X).
\end{lstlisting}

\section{Dettagli di Configurazione}
\label{sec:conf}

\subsection{File di Configurazione}

Il file \texttt{peshmind.json} definisce gli switch e i parametri di sistema in entrambe le modalità 
(di analisi e simulazione). Ecco un esempio di configurazione:

\begin{lstlisting}[language=json]
{
  "debug": true,
  "endpoint": "http://localhost:3030/pengine",
  "switches": {
    "switch-01": {
      "name": "switch-01",
      "mac": "aabbccddeeff",
      "description": "Core Switch Piano 1",
      "ip": "192.168.1.10",
      "username": "admin",
      "password": "ask",
      "model": "HP-Aruba",
      "port": 48,
      "extraports": [],
      "data": ""
    }
  }
}
\end{lstlisting}

\begin{itemize}
    \item \textbf{debug}: Abilita output verboso
    \item \textbf{endpoint}: URL dell'endpoint Pengines HTTP
    \item \textbf{switches}: Dizionario di configurazioni switch
    \begin{itemize}
        \item \textbf{name}: Identificatore univoco dello switch
        \item \textbf{mac}: Indirizzo MAC dello switch (senza separatori)
        \item \textbf{description}: Descrizione leggibile
        \item \textbf{ip}: Indirizzo IP di gestione
        \item \textbf{username}: Nome utente SSH/telnet
        \item \textbf{password}: Password SSH/telnet (usa "ask" per input interattivo)
        \item \textbf{model}: Modello switch (per template comandi)
        \item \textbf{port}: Numero di porte fisiche
        \item \textbf{extraports}: Porte aggiuntive (per es. trunk, uplink, ecc.)
    \end{itemize}
\end{itemize}

\subsection{Modalità Analisi Rete Esistente}
Per analizzare una rete esistente, è necessario solamente configurare correttamente il file \texttt{peshmind.json} 
con le informazioni di accesso per ogni switch gestito.

\subsection{Modalità Simulazione}

Nel caso della simulazione, in aggiunta alla configurazione, è necessario creare un file DOT che descriva la topologia
di rete desiderata. Il file DOT deve seguire una struttura specifica per essere analizzato correttamente dal sistema
di simulazione.
La modalità di simulazione permette di testare topologie usando file DOT. Il sistema:

\begin{enumerate}
    \item Analizza il grafo DOT usando la libreria \texttt{go-graphviz}
    \item Identifica nodi switch (normali e ghost)
    \item Estrae le connessioni dagli archi del grafo
    \item Genera MAC address fittizi per dispositivi end-node
    \item Produce fatti Prolog equivalenti
    \item Supporta i seguenti attributi speciali che possono essere aggiunti ai nodi:
    \begin{itemize}
        \item \texttt{ghost="true"} - Specifica che il nodo è un ghost switch
        \item \texttt{mac="..."} - Specifica MAC per ghost switch
        \item \texttt{port="N"} - Specifica porta di connessione
        \item \texttt{ports="N"} - Numero di porte per ghost switch
    \end{itemize}
\end{enumerate}

Un esempio di file DOT per la simulazione è mostrato nella sezione \ref{sec:examples}.

\section{Esempi di Utilizzo}
\label{sec:examples}

\subsection{Simulazione di una rete a 3 Switch}

Supponiamo di voler simulare una rete con 3 switch. Il file DOT, chiamiamolo \texttt{topology3.dot}, potrebbe essere il seguente: \\
\begin{lstlisting}[language=DOT]
    digraph dsgraph {
	node [label="\N"];
	"switch-0-1";
	"switch-0-2";
	"switch-3-5";
	"switch-0-1" -> "switch-0-2" [port = 24]; 
	"switch-0-2" -> "switch-0-1" [port = 24];
	"switch-0-1" -> "switch-3-5" [port = 23];
	"switch-3-5" -> "switch-0-1" [port = 23];
}
\end{lstlisting}

In questo esempio, abbiamo tre switch (switch-0-1, switch-0-2, switch-3-5) dei quali uno è centrale (switch-0-1) connesso agli 
altri due. \\

Eseguendo il comando di simulazione, otteniamo l'output mostrato di seguito, che include l'identificazione degli switch,
le porte di connessione e la generazione dei fatti Prolog corrispondenti. \\
\begin{lstlisting}[language=bash]
# Simulazione di topologia da file DOT
% peshmind simulate --config peshmind.json -d simout.dot -o kbpool -s topology3.dot --sim-generate-percentage 30
# Output di debug:
Found switch in graph: switch-0-1
Switch switch-0-1 has 24 ports and extra ports: []
Found switch in graph: switch-0-2
Switch switch-0-2 has 24 ports and extra ports: []
Found switch in graph: switch-3-5
Switch switch-3-5 has 24 ports and extra ports: []
Edge name: %3 from switch-0-1 to switch-0-2 port: 24
Edge name: %7 from switch-0-1 to switch-3-5 port: 23
Edge name: %1 from switch-0-2 to switch-0-1 port: 24
Edge name: %5 from switch-3-5 to switch-0-1 port: 23
Switch ports mapping: map[switch-0-1:map[23:switch-3-5 24:switch-0-2] switch-0-2:map[24:switch-0-1] switch-3-5:map[23:switch-0-1]]
DOT file written to simout.dot
Output file written to kbpool/switch-0-1.pl
Output file written to kbpool/switch-0-2.pl
Output file written to kbpool/switch-3-5.pl
\end{lstlisting}

Il file DOT generato (\texttt{simout.dot}), in figura \ref{fig:simout3}, rappresenta la topologia simulata, mentre i file Prolog nella directory
\texttt{kbpool} contengono i fatti generati per ogni switch, pronti per essere caricati nel motore di inferenza. \\

\begin{figure}[ht]
    \centering
    \includegraphics[width=.9\textwidth]{img/simout3.png}
    \caption{Visualizzazione della topologia simulata a 3 switch}
    \label{fig:simout3}
\end{figure}

Il files generati dal simulatore sono utilizzabili nella modalità di analisi. Il comando \texttt{serve} carica i fatti
nella knowledge base e avvia il motore di inferenza, permettendo di eseguire query per identificare la topologia. \\

\begin{lstlisting}[language=bash]
# Avvio del motore Prolog con i fatti simulati
peshmind serve --kbpool kbpool
\end{lstlisting}

Una volta avviato il motore, è possibile eseguire query come quelle mostrate di seguito. \\

\begin{lstlisting}[language=bash]
# Query per identificare switch esterni (edgeswitch)
% peshmind query "edgeswitchpn(X,N)"
# Output:
&{edgeswitchpn [switch-0-2 24]}
&{edgeswitchpn [switch-0-2 24]}
&{edgeswitchpn [switch-3-5 23]}
&{edgeswitchpn [switch-3-5 23]}

# Query per identificare switch collegati direttamente
% peshmind query "directpn(X,Y,PORTX,PORTY)"
# Output:
&{directpn [switch-0-1 switch-0-2 24 24]}
&{directpn [switch-0-1 switch-3-5 23 23]}
&{directpn [switch-0-2 switch-0-1 24 24]}
&{directpn [switch-3-5 switch-0-1 23 23]}
\end{lstlisting}

Infine, è possibile generare una visualizzazione grafica,
mostrata in figura \ref{fig:inferred3},
della topologia inferita usando il comando \texttt{dot}. \\
\begin{lstlisting}[language=bash]
# Generazione file DOT da topologia inferita
peshmind dot > inferred_topology.dot
dot -Tpng inferred_topology.dot -o inferred_topology.png
\end{lstlisting}

\begin{figure}[ht]
    \centering
    \includegraphics[width=.3\textwidth]{img/inferred3.png}
    \caption{Visualizzazione della topologia inferita a 3 switch}
    \label{fig:inferred3}
\end{figure}

Come si vede la topologia inferita corrisponde esattamente a quella simulata, con switch-0-1 identificato come switch
di core e switch-0-2 e switch-3-5 identificati come switch di accesso, con le porte di interconnessione correttamente evidenziate. \\

\subsection{Simulazione di una stella a 6 switch}

Partendo da una topologia a stella con 6 switch come quella mostrata nel file \texttt{topology6star.dot} di seguito, è 
possibile eseguire la simulazione e l'inferenza per identificare la topologia. \\

\begin{lstlisting}[language=DOT]
digraph dsgraph {
	node [label="\N"];
	"switch-0-1";
	"switch-0-2";
	"switch-0-3";
	"switch-3-5";
	"switch-3-30";
	"switch-3-31"; 
	"switch-3-5" -> "switch-0-2" [port = 22]; 
	"switch-0-2" -> "switch-3-5" [port = 22];
	"switch-3-5" -> "switch-3-30" [port = 24];
	"switch-3-30" -> "switch-3-5" [port = 24];
	"switch-0-1" -> "switch-3-5" [port = 23];
	"switch-3-5" -> "switch-0-1" [port = 23];
	"switch-0-3" -> "switch-3-5" [port = 21];
	"switch-3-5" -> "switch-0-3" [port = 21];
	"switch-3-31" -> "switch-3-5" [port = 20];
	"switch-3-5" -> "switch-3-31" [port = 20];
}
\end{lstlisting}

In questa configurazione, switch-3-5 è il nodo centrale
connesso a 5 switch (switch-0-1, switch-0-2, switch-0-3, 
switch-3-30, switch-3-31). La struttura generata utilizzando il comando: \\

\begin{lstlisting}[language=bash]
peshmind simulate --config peshmind.json -d simout6star.dot -o kbpool -s topology6star.dot --sim-generate-percentage 30
\end{lstlisting}

è mostrata in figura \ref{fig:simout6star}. \\

\begin{figure}[ht]
    \centering
    \includegraphics[width=.9\textwidth]{img/simout6star.png}
    \caption{Visualizzazione della topologia simulata a stella con 6 switch}
    \label{fig:simout6star}
\end{figure}

Come nel caso precedente, è possibile avviare il motore Prolog con i fatti generati e eseguire query per identificare 
la topologia inferita, che dovrebbe corrispondere esattamente a quella simulata, con switch-3-5 identificato come switch
di core e gli altri switch identificati come switch di accesso. \\

\begin{lstlisting}
# Avvio del motore Prolog con i fatti simulati
% peshmind serve --kbpool kbpool
# Query per identificare switch core
% peshmind query "internalswitchn(X)"
# Output:
&{internalswitchn [switch-3-5]}
# Query per identificare switch di accesso
% peshmind query "edgeswitchn(X)"
# Output:
&{edgeswitchn [switch-0-1]}
&{edgeswitchn [switch-0-2]}
&{edgeswitchn [switch-0-3]}
&{edgeswitchn [switch-3-30]}
&{edgeswitchn [switch-3-31]}
# Switch di accesso e relative porte di connessione
% peshmind query "edgeswitchpn(X,N)"
# Output:
&{edgeswitchpn [switch-0-1 23]}
&{edgeswitchpn [switch-0-2 22]}
&{edgeswitchpn [switch-0-3 21]}
&{edgeswitchpn [switch-3-30 24]}
&{edgeswitchpn [switch-3-31 20]}
\end{lstlisting}

Come si vede, la topologia inferita corrisponde esattamente a quella simulata, con switch-3-5 identificato come switch
di core e gli altri switch identificati come switch di accesso, con le porte di interconnessione correttamente evidenziate. \\
Per visualizzare la topologia inferita, è possibile generare un file DOT usando il comando \texttt{dot} e convertirlo in PNG.
Il cui risultato è mostrato in figura \ref{fig:inferred6star}. \\

\begin{lstlisting}[language=bash]
# Generazione file DOT da topologia inferita
peshmind dot > inferred_topology6star.dot
fdp -Tpng inferred_topology6star.dot -o inferred_topology6star.png
\end{lstlisting}

\begin{figure}[ht]
    \centering
    \includegraphics[width=.5\textwidth]{img/inferred6star.png}
    \caption{Visualizzazione della topologia inferita a stella con 6 switch}
    \label{fig:inferred6star}
\end{figure}

\subsection{Simulazione di uno switch non censito}

Il simulatore permette di specificare switch non censiti (ghost switch) usando l'attributo \texttt{ghost="true"} nei nodi del file
DOT. Ad esempio, nel file \texttt{topology4ghost.dot} mostrato di seguito, privsw-0-1g è un ghost switch. Quando
il simulatore processa questo oggetto, genera fatti Prolog senza che venga dichiarato come switch, permettendo di verificare se il
sistema di inferenza riesce a identificare correttamente la presenza del ghost switch partendo solo dalle informazioni
degli altri switch. \\

\begin{lstlisting}[language=DOT]
    digraph dsgraph {
	node [label="\N"];
	"privsw-0-1g" [ghost = true, ports = 24, mac = "004000000001"];
	"privsw-0-2";
	"privsw-3-5";
	"privsw-3-30";
	"privsw-0-1g" -> "privsw-0-2" [port = 24]; 
	"privsw-0-2" -> "privsw-0-1g" [port = 24];
	"privsw-0-1g" -> "privsw-3-5" [port = 23];
	"privsw-3-5" -> "privsw-0-1g" [port = 23];
	"privsw-3-5" -> "privsw-3-30" [port = 24];
	"privsw-3-30" -> "privsw-3-5" [port = 24];
}
\end{lstlisting}

Simulando questa topologia e avviando il motore Prolog, 
è possibile eseguire query per identificare la presenza
del ghost switch. Ad esempio, la query \texttt{ghostn(X,Y)} 
restituisce le coppie di switch direttamente connessi
che vedono lo stesso MAC su porte di interconnessione, 
indicando la presenza di un ghost switch tra di loro. \\

\begin{lstlisting}[language=bash]
# Simulazione di topologia con ghost switch
% peshmind simulate --config peshmind.json -d simout4ghost.dot -o kbpool -s topology4ghost.dot --sim-generate-percentage 30
# Avvio del motore Prolog con i fatti simulati
% peshmind serve --kbpool kbpool
# Query per identificare ghost switch
% peshmind query "ghostn(X,Y)"
# Output:
&{ghostn [privsw-0-2 privsw-3-5]}
\end{lstlisting}

Anche in questo caso, la topologia inferita  può essere
visualizzata graficamente, come mostrato in figura \ref{fig:inferred4ghost}, usando il comando \texttt{dot}, 
e dovrebbe evidenziare la presenza del ghost switch 
privsw-0-1g come nodo intermedio tra privsw-0-2 e
privsw-3-5. \\

\begin{figure}[ht]
    \centering
    \includegraphics[width=.5\textwidth]{img/inferred4ghost.png}
    \caption{Visualizzazione della topologia inferita con ghost switch}
    \label{fig:inferred4ghost}
\end{figure}

\subsection{Analisi di un caso con dati reali}

Come ultimo esempio, è stata eseguita l'analisi di una
rete reale composta da 6 switch, con dati raccolti tramite
il comando \texttt{update} e caricati nel motore Prolog. 
La configurazione degli switch è stata definita nel file
\texttt{peshmind.json} riportato di seguito. \\

\begin{lstlisting}[language=json]
{
	"debug": true,
	"endpoint": "http://localhost:3030/pengine",
	"switches": {
		"privsw-0-1": {
			"name": "privsw-0-1",
			"mac": "e4..",
			"description": "",
			"ip": "10...",
			"Username": "admin",
			"Password": "ask",
			"model": "HP-Aruba",
			"port": 24,
			"extraports": [],
			"data": ""
		},
		"privsw-0-2": {
			"name": "privsw-0-2",
			"mac": "00..",
			"description": "",
			"ip": "10...",
			"Username": "admin",
			"Password": "ask",
			"model": "HP-Aruba",
			"port": 24,
			"extraports": [],
			"data": ""
		},
		"privsw-200-1": {
			"name": "privsw-200-1",
			"mac": "00..",
			"description": "",
			"ip": "10...",
			"Username": "admin",
			"Password": "ask",
			"model": "HP-Aruba",
			"port": 24,
			"extraports": [],
			"data": ""
		},
		"privsw-200-2": {
			"name": "privsw-200-2",
			"mac": "00..",
			"description": "",
			"ip": "10...",
			"Username": "admin",
			"Password": "ask",
			"model": "HP-Aruba",
			"port": 24,
			"extraports": [],
			"data": ""
		},
		"privsw-2-1": {
			"name": "privsw-2-1",
			"mac": "00..",
			"description": "",
			"ip": "10...",
			"Username": "admin",
			"Password": "ask",
			"model": "HP-Aruba",
			"port": 24,
			"extraports": [],
			"data": ""
		},
		"privsw-3-5": {
			"name": "privsw-3-5",
			"mac": "00..",
			"description": "",
			"ip": "10...",
			"Username": "admin",
			"Password": "ask",
			"model": "HP-Aruba-Press",
			"port": 24,
			"extraports": [],
			"data": ""
		},
		"privsw-3-30": {
			"name": "privsw-3-30",
			"mac": "e00..",
			"description": "",
			"ip": "10...",
			"Username": "admin",
			"Password": "ask",
			"model": "HP-Aruba",
			"port": 24,
			"extraports": [],
			"data": ""
		}
	}
}
\end{lstlisting}

Al contrario dei casi di simulazione, in questo scenario
non viene fornita alcuna informazione sulla topologia di
rete, ma solamente le credenziali di accesso agli switch
per recuperare le tabelle MAC. \\

\begin{lstlisting}[language=bash]
# Aggiornamento dati switch reali
% peshmind update --switch all
# Output:
Updating switch: privsw-0-1
....
# Avvio del motore Prolog con i fatti reali
% peshmind serve --kbpool kbpool
\end{lstlisting}

I dati raccolti dagli switch reali possono essere 
incompleti o rumorosi, con informazioni mancanti o
contraddittorie. Nonostante ciò, il motore di inferenza
riesce a identificare correttamente la topologia di rete,
e a evidenziare eventuali anomalie o switch non censiti. \\
Il risultato dell'inferenza può essere visualizzato
graficamente, come mostrato in figura \ref{fig:inferredreal}, usando il comando \texttt{dot}. \\
\begin{lstlisting}[language=bash]
# Generazione file DOT da topologia inferita
peshmind dot > inferred_real.dot
dot -Tpng inferred_real.dot -o inferred_real.png
\end{lstlisting}    

\begin{figure}[h]
    \centering
    \includegraphics[width=.6\textwidth]{img/inferredreal.png}
    \caption{Visualizzazione della topologia inferita da dati reali}
    \label{fig:inferredreal}
\end{figure}

Lo switch ghost rilevato tra privsw-3-5 e privsw-3-30
indica la presenza di un dispositivo non censito che
interconnette questi due switch. Infatti, nella rete 
reale, esiste uno switch di core che volutamente non è 
stato inserito nella configurazione, ma che viene 
correttamente identificato come ghost switch grazie
alle informazioni raccolte dagli switch di accesso. \\

\section{Limitazioni e Sviluppi Futuri}

\subsection{Limitazioni Attuali}

\begin{itemize}
    \item Supporto limitato di modelli di switch, ogni modello ha bisogno di un template specifico
    \item Richiede accesso SSH/console agli switch
    \item Non gestisce VLAN multiple in modo esplicito
\end{itemize}

\subsection{Possibili Sviluppi Futuri}

\begin{enumerate}
    \item \textbf{Query più esaustive}
    \begin{itemize}
        \item Aggiungere template per un maggior numero di modelli di switch
        \item Supporto SNMP come alternativa a SSH
    \end{itemize}
    
    \item \textbf{Analisi VLAN}
    \begin{itemize}
        \item Inferire configurazioni VLAN
        \item Identificare errori di configurazione VLAN
    \end{itemize}
    
    \item \textbf{Interface Web}
    \begin{itemize}
        \item Dashboard per visualizzazione interattiva
        \item Editor grafico topologie
        \item Monitoring real-time
    \end{itemize}
    
    \item \textbf{Integrazione}
    \begin{itemize}
        \item Export verso NetBox, LibreNMS
        \item API REST per integrazione
        \item Webhook per notifiche cambiamenti
    \end{itemize}
\end{enumerate}

\section{Conclusioni}

peshmind rappresenta un approccio innovativo alla scoperta automatica di topologie di rete, combinando l'efficienza di Go con la potenza dell'inferenza logica Prolog. Il sistema si è dimostrato efficace nell'identificare automaticamente:

\begin{itemize}
    \item Connessioni fisiche tra switch
    \item Switch non documentati (ghost switch)
    \item Classificazione switch (core vs. accesso)
    \item Anomalie nella topologia di rete
\end{itemize}

La combinazione di un'interfaccia CLI intuitiva con un motore di ragionamento logico fornisce agli amministratori di rete uno strumento potente per comprendere, documentare e validare le loro infrastrutture di rete.

Il progetto è rilasciato con licenza Apache 2.0, favorendo l'adozione e la contribuzione della community open source.

\section{Bibliografia e Riferimenti}

\begin{itemize}
    \item SWI-Prolog Documentation: \url{https://www.swi-prolog.org/}
    \item Pengines Library: \url{https://www.swi-prolog.org/pldoc/man?section=pengines}
    \item Cobra CLI Framework: \url{https://github.com/spf13/cobra}
    \item Graphviz Documentation: \url{https://graphviz.org/}
    \item Go Programming Language: \url{https://golang.org/}
\end{itemize}

\appendix

\end{document}
